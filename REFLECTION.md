I used what I've learned to create a piece of code that takes a user inputed email and saves it to a .txt file. 

Use cases for sometihng like this are noumerous but main applications can include signing up for newsletters / email lists, putting an email down for a reciept or it can be adapted to input any kind of data and create a file containing it. 

I have tried to use everything I can from what I have learnt from this course like; if / else statements, importing libries / making my own, reading and writing to files, printing code, taking user inputs, using reg ex to ensure correct formats, while loops, creating functions, testing code with pytest. The main bulk of my code for this submision is using knowlege from lectures 7-10 but previous lectures played a role too in forming fundamental programming skills that I needed to even start this project. 

I started the process with creating my own library that i could import that would ask for your name and then print "hello", name. I then moved onto the file I/O implamentation so i could read and write names to a .txt file. Next i made the test enviroment to test that the read file names where definately the last entry. I will cover exactly how I tested this in the video. After testin I then pivoted to storeing emails so I could better show off the use of reg ex while also broadening the scop of the codes applications rather than just saving names. 

Things to note in my code, I added an option to refuse to have your email saved and while it may not be what was neccasseraly asked I thought user protection would be a good thing. I also added code so that the system can loop if you want to enter multiple emails. 

